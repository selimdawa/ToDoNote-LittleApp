<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MainApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="checkboxStyle">@style/Widget.App.CheckBox</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/basic_night</item>
        <item name="colorAccent">@color/basic_night</item>
        <item name="color">@color/white</item>
        <item name="background">@color/basic_night</item>
        <item name="tickColor">@color/basic_night</item>
        <item name="trackColor">@color/basic_night</item>
        <item name="colorOnBackground">@color/dark</item>
        <item name="colorError">@color/white</item>
        <item name="colorOnError">@color/dark</item>
    </style>

    <style name="Theme.MainApp" parent="Base.Theme.MainApp" />

    <!-- Define the checkbox specific theme override -->
    <style name="Widget.App.CheckBox" parent="Widget.Material3.CompoundButton.CheckBox">
        <!-- This will tint the checkbox to white -->
        <item name="buttonTint">@color/white</item>
    </style>
</resources>